"use strict";
class Friend {
    constructor(firstName, lastName, city, state, pictureUrl) {
        this.name = `${firstName} ${lastName}`;
        this.location = { city: city, state: state };
        this.pictureUrl = pictureUrl;
    }
}
//# sourceMappingURL=friend.js.map